import type { AppState, UserProfile } from '../types'
export default function Settings({state,onBackup,onRestore,onReset,onTheme,onExitDemo}:{state:AppState;onBackup:()=>void;onRestore:(f:File)=>void;onReset:()=>void;onTheme:(t:UserProfile['theme'])=>void;onExitDemo:()=>void}){
  const p=state.profile
  return <main className="page"><header><p className="eyebrow">LOCAL-FIRST</p><h1>Cài đặt</h1></header>{p&&<section className="card"><h3>{p.name}</h3><p className="muted">{p.trainingType==='calisthenics'?'Calisthenics':p.trainingType==='home'?'Tạ đơn tại nhà':'Phòng Gym'} · {p.daysPerWeek} buổi/tuần · {p.sessionMinutes} phút</p><div className="seg"><button className={p.theme==='light'?'active':''} onClick={()=>onTheme('light')}>Sáng</button><button className={p.theme==='dark'?'active':''} onClick={()=>onTheme('dark')}>Tối</button></div></section>}<section className="card"><h3>Dữ liệu cá nhân</h3><p className="muted">Dữ liệu tập luyện nằm trong trình duyệt trên thiết bị này và không tự động tải lên máy chủ.</p><button className="btn secondary full" onClick={onBackup}>Xuất Backup JSON</button><label className="btn secondary full file-btn">Nhập Restore JSON<input type="file" accept="application/json" onChange={e=>e.target.files?.[0]&&onRestore(e.target.files[0])}/></label></section><section className="card warning"><b>Lưu ý khi đổi điện thoại</b><p>Máy mới sẽ không có dữ liệu của máy cũ. Hãy xuất Backup JSON trước khi đổi máy hoặc xoá dữ liệu Safari/Chrome.</p></section>{state.demoMode&&<button className="btn secondary full" onClick={onExitDemo}>Thoát Demo và tạo hồ sơ thật</button>}<button className="btn danger-btn full" onClick={onReset}>Xoá toàn bộ dữ liệu trên thiết bị này</button></main>
}
